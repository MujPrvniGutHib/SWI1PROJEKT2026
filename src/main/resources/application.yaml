spring:
  application:
    name: SWI126
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: create-drop
app:
  name: MyAwesomeBackend
server:
  port: 8081